services:
  postgres:
    image: postgres:16-alpine
    container_name: menuos-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: menuos
      POSTGRES_PASSWORD: menuos_secret
      POSTGRES_DB: menuos
    volumes:
      - menuos_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U menuos"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  menuos_pg_data:
